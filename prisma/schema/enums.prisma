// ================================
// User & Auth Related Enums
// ================================

enum userRole {
  USER
  ADMIN
  MODERATOR
  SUPERADMIN
}

enum userStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  DELETED
  BLOCKED
}

enum mfaMethod {
  totp
  sms
  email
  webauthn
}

enum loginAction {
  login
  logout
}

// ================================
// Email Related Enums
// ================================

enum email_type {
  verification
  password_reset
  notification
}

enum email_provider {
  sendgrid
  mailgun
  ses
  stmp
}

enum email_status {
  sent
  failed
  pending
  bounced
  delivered
  opened
  clicked
}

// ================================
// Activity Related Enums
// ================================

enum eventType {
  create
  update
  delete
  login
  logout
  password_change
  profile_update
}

enum actionType {
  create
  update
  delete
}

enum MakeupSessionStatus {
  CREATED
  IMAGE_UPLOADED
  ANALYZING
  RECOMMENDATIONS_READY
  METHOD_SELECTED
  GUIDE_GENERATING
  GUIDE_READY
  IN_PROGRESS
  COMPLETED
  ANALYSIS_FAILED
  GUIDE_FAILED
}

enum ImagePurpose {
  INITIAL_ANALYSIS
  STEP_CHECK
  COMPLETION
}

enum ImageSource {
  MOCK_CAPTURE
  PRIVATE_UPLOAD
}

enum AiOperation {
  PERSONALIZATION
  IMAGE_GENERATION
  GUIDE_GENERATION
  VISION_CHECK
  GUIDE_QUESTION
}

enum AiRunStatus {
  QUEUED
  PROCESSING
  COMPLETED
  FAILED
}

enum GuideStatus {
  GENERATING
  READY
  IN_PROGRESS
  COMPLETED
}

enum GuideStepStatus {
  LOCKED
  CURRENT
  COMPLETED
  SKIPPED
}

enum EvaluationResult {
  PASS
  NEEDS_ADJUSTMENT
  UNCERTAIN
  CANNOT_EVALUATE
}
